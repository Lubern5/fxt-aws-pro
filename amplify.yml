version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm install -g npm@latest
        - cd frontend
        - npm install tailwindcss@4.1.16 @tailwindcss/postcss postcss autoprefixer
        - npm ci || true
    build:
      commands:
        - echo "⚙️ Building Next.js app with local Tailwind v4 support"
        # ❌ remove cd frontend (Amplify is already in that folder)
        - NODE_PATH=$(pwd)/node_modules npm run build
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
      - frontend/.next/cache/**/*
